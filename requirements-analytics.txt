# Minimal runtime dependencies for services/analytics.
# Keep this file independent from requirements.txt to avoid pulling API,
# OpenStack, database, Redis, and document-generation dependencies.
grpcio==1.68.1
protobuf==5.29.2
python-dotenv==1.1.1
requests==2.32.5
pydantic==2.12.5
pandas==2.3.3
numpy==1.26.4

# Provides autogluon.timeseries without installing the full autogluon meta-package.
autogluon.timeseries
